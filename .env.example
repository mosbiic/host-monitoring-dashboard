# Host Monitoring Dashboard Environment Configuration

# Cloudflare Access SSO 配置
# 设置为 true 启用 Cloudflare Access 认证，false 使用本地 Token
CF_ACCESS_ENABLED=false

# Authentication Token (本地开发模式使用)
DASHBOARD_TOKEN=changeme

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8080

# Frontend Configuration
VITE_API_BASE=http://localhost:8080
VITE_WS_HOST=localhost:8080
